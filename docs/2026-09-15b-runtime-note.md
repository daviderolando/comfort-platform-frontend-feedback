# Runtime Note

This fresh Angular 21 Material shell uses Node `24.21.0`.

```sh
cd /Users/drolando/Documents/Dev/kth/kth-comfort-platform/frontend-feedback
nvm use
npm start
```

The old Angular 11 reference app should be run with Node `20.18.0`; it is not compatible with Node 24's dev-server runtime.
